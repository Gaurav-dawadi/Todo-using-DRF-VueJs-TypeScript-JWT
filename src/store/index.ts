import Vue from 'vue'
import Vuex from 'vuex'
import authUser from './modules/authUser'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    authUser,
  }
})
